## Installation

The project uses pixi to organize dependencies and Python version.

- Install pixi from <https://pixi.prefix.dev/latest/installation/>
- Open pixi shell with `pixi shell`
- Test installation by running `python run_manual.py`
